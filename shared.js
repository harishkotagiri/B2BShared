window.getFullName = (fname, lname) => {
    return `${fname} ${lname}`;
}

window.calculateVals = (val1, val2) => {
    return val1+val2;
}

const testFunc = () => {
    return "Called test function"
}
