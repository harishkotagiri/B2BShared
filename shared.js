window.getFullName = (fname, lname) => {
    return `${fname} ${lname}`;
}

const calculateVals = (val1, val2) => {
    return val1+val2;
}

window.testFunc = () => {
    return "Called test function"
}
